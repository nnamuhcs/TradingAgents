apiVersion: v1
kind: Secret
metadata:
  name: tradingagents-secrets
  namespace: tradingagents
  labels:
    app.kubernetes.io/name: tradingagents
type: Opaque
stringData:
  # GitHub token for Copilot API access
  GITHUB_TOKEN: ""
  # Optional: direct provider keys (uncomment if needed)
  # OPENAI_API_KEY: ""
  # ANTHROPIC_API_KEY: ""
  # GOOGLE_API_KEY: ""
